# Environment variables (never commit real secrets)
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Logs
logs/
*.log

# OS files
.DS_Store
Thumbs.db

# Docker volumes
postgres_data/

# Uploaded files (managed on server, not in git)
backend/public/uploads/

# Temp files
backend/tmp/

# Tessera assets: ora committati (servono al backend per generare il PDF tessera).
# Font BethEllen (OFL) + template PDF dell'associazione.

# Build output
# I frontend sono compilati in backend/public/{site,admin} e COMMITTATI:
# il deploy del solo backend (Express) li serve come statici sullo stesso dominio
# (sito pubblico alla root, MVP sotto /admin). backend/public/uploads resta ignorato.

# Lock files (generated)
package-lock.json

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
