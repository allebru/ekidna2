# API base URL (build-time, Vite)
# "/api" relativo: funziona in dev (proxy Vite) e in prod (reverse-proxy)
VITE_API_BASE_URL=/api

# Backend su dominio separato:
# VITE_API_BASE_URL=https://api.ekidna.org/api
