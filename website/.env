# API base URL usato al build da Vite.
# '/api' (relativo) funziona sia in dev (proxy di Vite -> :3001)
# sia in produzione (reverse-proxy .htaccess -> backend Node).
# Per usare un backend su dominio separato: https://api.ekidna.org/api
VITE_API_BASE_URL=/api
