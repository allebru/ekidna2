{
  "name": "ekidna-deploy",
  "version": "1.0.0",
  "private": true,
  "description": "Entry di deploy per Hostinger: installa e avvia il backend Express, che serve anche i frontend gia' compilati (sito pubblico alla root, MVP su /admin).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "npm install --prefix backend --omit=dev",
    "build": "npm install --prefix backend --omit=dev",
    "start": "node backend/src/server.js"
  }
}
